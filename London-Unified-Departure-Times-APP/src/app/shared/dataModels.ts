export interface BusLine{
    id?: string;
    name?: string;
  }
  
export interface Station{
    id?: string;
    name?: string;
  }

export interface Schedule{
  date?: string;
  first?: string;
  last?: string;
}